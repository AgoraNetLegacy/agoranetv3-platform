# Known issues

Add one time-bounded Markdown record per active incident. The corpus compiler requires incident status, affected versions, verification time, workaround status, and next-update time for every known-issue document.
